services:
  server:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - tuneboss-data:/app/data
    env_file: .env
    environment:
      TOKEN_PATH: /app/data/.tokens.json

volumes:
  tuneboss-data:
